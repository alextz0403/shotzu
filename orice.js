let randomNr = Math.trunc(Math.random() * 100) + 1;

const probabilitate = (p1, p2) =>
  console.log(`${p1} are ${randomNr}% sanse cu ${p2}`);

probabilitate("Catalin", "orice femeie pe planeta asta");
